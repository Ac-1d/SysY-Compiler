package node;

public class BlockNode {
    // Block → '{' { BlockItem } '}'

    public static BlockNode Block() {
        BlockNode blockNode = new BlockNode();

        return blockNode;
    }
}
